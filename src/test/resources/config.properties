# Mobile Test Automation App - Configuration (TRIPLE HYBRID: Mobile + Web + API)
# This file contains all configuration properties for the test automation framework

# ============ TEST TYPE ============
# Set to 'mobile' for Appium/mobile tests, 'web' for Selenium/web tests, or 'api' for REST API tests
test.type=api

# ============ ENVIRONMENT ============
environment=staging
platform=android

# ============ APPLICATION ============
app.name=TestApp
app.path=/path/to/app.apk
app.version=1.0.0

# ============ WEB CONFIGURATION ============
browser=chrome
web.base.url=http://localhost:3000
headless.mode=false
incognito.mode=false
page.load.timeout=30

# ============ APPIUM SERVER ============
appium.server.url=http://localhost
appium.server.port=4723
appium.start.retry=3
appium.start.timeout=30000
appium.command.timeout=60000

# ============ ANDROID CAPABILITIES ============
android.device.name=emulator-5554
android.platform.version=12
android.app.package=com.example.testapp
android.app.activity=.splash.SplashActivity
android.automation.name=UiAutomator2
android.no.reset=false
android.full.reset=true

# ============ iOS CAPABILITIES ============
ios.device.name=iPhone Simulator
ios.platform.version=16.0
ios.automation.name=XCUITest
ios.bundle.id=com.example.testapp

# ============ WAIT CONFIGURATIONS ============
implicit.wait=10
explicit.wait=15
polling.interval.ms=500

# ============ SCREENSHOT CONFIGURATION ============
screenshot.on.failure=true
screenshot.path=target/screenshots
screenshot.format=png

# ============ JIRA INTEGRATION (OPTIONAL) ============
jira.enabled=false
jira.url=https://your-jira.atlassian.net
jira.user=user@example.com
jira.token=your_api_token
jira.project.key=PROJ
max.retry=2

# ============ LOGGING ============
log.level=INFO
log.file.path=target/logs
log.file.name=test-automation.log

# ============ REPORT CONFIGURATION ============
allure.enabled=true
allure.results.path=target/allure-results

# ============ PARALLEL EXECUTION ============
parallel.classes=false
parallel.methods=false
parallel.threads=1

# ============ API CONFIGURATION ============
api.base.url=http://localhost:8080/api
api.version=v1
api.auth.required=false
api.auth.type=bearer
api.auth.token=your_api_token_here
api.auth.username=admin
api.auth.password=password123
api.timeout=30
api.logging.enabled=true

